@model List<Website_BanMayTinh.Models.Category>

<ul class="dropdown-menu">
    @foreach (var category in Model)
    {
        <li>
            <a class="dropdown-item" href="/Home/Index?categoryId=@category.Id">
                @category.Name
            </a>
        </li>
    }
</ul>
